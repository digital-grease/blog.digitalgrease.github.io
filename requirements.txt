# Build core: ProperDocs (drop-in fork of MkDocs 1.x, avoids the MkDocs 2.0 break)
# mkdocs-material still pulls in mkdocs as a transitive dep; both coexist.
properdocs>=1.6.7
mkdocs-material>=9.5.0

# Blog Plugin (included in Material for MkDocs Insiders or as standalone)
mkdocs-material[blog]>=9.5.0

# Additional Plugins
mkdocs-minify-plugin>=0.8.0
mkdocs-redirects>=1.2.1
mkdocs-rss-plugin>=1.12.0

# Required for advanced features
pillow>=10.0.0
cairosvg>=2.7.0

# Markdown extensions
pymdown-extensions>=10.7
